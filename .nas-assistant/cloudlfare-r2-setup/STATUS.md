# Status

## Now
Locking object key naming scheme: `<env>/<scope>/<kind>/<id>.<ext>` proposed for R2 objects.

## Next
User confirms key naming format, then implement key generation and auth validation.

## Blocked on
User confirmation on object key structure (env, scope, kind, id, ext components).
