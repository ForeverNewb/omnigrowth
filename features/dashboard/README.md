# Dashboard feature

Mirrors `docs/features/dashboard.md`. App shell + Quick AI Post Generator
widget + today's posts queue + OmniBits balance + insight list. Mostly
composes panels exported from other features.
