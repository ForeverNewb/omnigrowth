# Landing feature

Mirrors `docs/features/landing.md`. One-pager marketing site at `/`.
Structure:

- `components/` — section components (Hero, Features, LiveDemo, Compare, Pricing, FAQ, CTA, Footer)
- `lib/` — small helpers (none yet)
- `schema.ts` — empty (no persistence)
